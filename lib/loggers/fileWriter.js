'use strict';

module.exports = exports = function (options) {

	/*
	* Accepts:
	*   stream
	*   format
	*   */

	return function (msg, next) {

		// format

		// check existence of message

		// write to stream
		stream.write(line + '\n', 'ascii');

		next();

	};

};